services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_DATABASE: otus
      MYSQL_USER: mysql
      MYSQL_ROOT_PASSWORD: root
    ports:
      - 3306:3306